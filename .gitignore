# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit real secrets
.env
.env.local
.env.*.local
.env.production

# uploads (served from disk, backed up separately)
/public/uploads/*
!/public/uploads/.gitkeep

# generated fallback OG image is committed on purpose (scripts/make-og-default.ts)

# prisma local
/database/migrations/dev.db*
